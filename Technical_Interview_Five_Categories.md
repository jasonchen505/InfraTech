# 技术面试五类问题应对指南

> 基于 InfraTech 项目深度分析，针对 LLM 算法实习面试的五类核心能力

---

## 第一类：底层原理深入理解

> **核心要求**：不只是回答清楚概念，而是讲清楚方法解决什么问题、存在哪些局限性、有哪些改进方法

### 1.1 面试官追问模式

面试官通常会问："你讲讲这个技术的原理"，然后根据你的回答层层深入：
- "它解决了什么问题？"
- "为什么这样设计，而不是其他方式？"
- "有什么局限性？"
- "如果是你，你会怎么改进？"

### 1.2 示例回答框架

#### 示例1：MLA（Multi-head Latent Attention）

**面试官**：讲讲 DeepSeek V3 的 MLA 机制？

**回答结构**：

**第一层：解决什么问题**
> 标准 MHA 的 KV Cache 随序列长度线性增长，128 头 × 128 维 × 2（K和V）× 层数，在长上下文场景下显存压力巨大。MQA/GQA 通过共享 KV 头来减少 Cache，但会损失表达能力。MLA 的目标是在不损失表达能力的前提下，大幅压缩 KV Cache。

**第二层：核心原理**
> MLA 的核心思想是将 KV 投影到低维潜空间。具体来说：
> 1. 将隐状态 h 通过下投影矩阵压缩到低维向量 c（512 维）
> 2. 推理时只需缓存 c，而非完整的 K、V
> 3. 通过上投影矩阵从 c 恢复 K、V
> 4. 使用"吸收矩阵"技巧，将上投影吸收到 Q 中，避免显式恢复

**第三层：局限性**
> 1. **与 RoPE 的兼容问题**：RoPE 需要作用于 Q、K，但 MLA 的 K 是从潜空间恢复的，直接应用会破坏低秩结构。解决方案是额外添加 decoupled RoPE 分量
> 2. **训练复杂度**：虽然推理省显存，但训练时仍需计算完整的 K、V
> 3. **MHA/MQA 模式切换**：Prefill 和 Decode 阶段使用不同模式，增加了工程复杂度

**第四层：改进思路**
> 1. 可以探索更激进的压缩比（如 256 维），评估精度损失
> 2. 结合稀疏注意力（如 DSA），进一步减少计算量
> 3. 探索动态压缩比，根据序列长度自适应调整

#### 示例2：PagedAttention

**面试官**：vLLM 的 PagedAttention 是怎么工作的？

**第一层：解决什么问题**
> 传统推理框架为每个请求预分配最大长度的连续内存，导致严重的内存碎片化。例如，一个请求可能只用了 100 tokens，但分配了 2048 tokens 的空间。多个请求同时服务时，显存利用率可能只有 20-40%。

**第二层：核心原理**
> 借鉴操作系统的虚拟内存分页机制：
> 1. 将 KV Cache 分成固定大小的 Block（如 16 tokens）
> 2. 使用 Block Table 记录逻辑 Block 到物理 Block 的映射
> 3. Block 可以非连续存储，按需分配
> 4. 支持 Copy-on-Write，Beam Search 时多个候选共享相同前缀

**第三层：局限性**
> 1. **Block Size 的 trade-off**：太小则管理开销大，太大则内部碎片多
> 2. **碎片问题仍然存在**：最后一个 Block 可能有空闲 slots
> 3. **跨请求共享复杂**：需要引用计数机制
> 4. **与 Flash Attention 的集成**：需要特殊处理非连续内存访问

**第四层：改进思路**
> 1. 动态 Block Size：根据请求特征自适应调整
> 2. 层次化存储：热数据在 GPU，冷数据在 CPU
> 3. 压缩 KV Cache：结合量化或稀疏化

### 1.3 常见技术的"四层追问"准备表

| 技术 | 解决什么问题 | 核心原理 | 局限性 | 改进思路 |
|------|------------|---------|--------|---------|
| **RoPE** | 相对位置编码的外推性 | 旋转矩阵编码位置 | 长度外推需要 scaling | NTK-aware scaling, YaRN |
| **MoE** | 模型容量 vs 计算成本 | 稀疏激活专家 | 负载均衡、通信开销 | Auxiliary Loss-free, EP 优化 |
| **LoRA** | 全量微调成本高 | 低秩分解 | 表达能力受限 | 动态秩、LoRA+ |
| **Chunked Prefill** | 长 prompt 阻塞 decode | 分块交错执行 | 额外的 chunk 边界处理 | 自适应 chunk size |
| **投机推理** | 自回归解码慢 | 小模型提议+大模型验证 | 依赖 draft 模型质量 | 自适应 draft 长度 |

---

## 第二类：实验和方案验证能力

> **核心要求**：面试官关注你怎么证明方案有效，追问实验细节看是否有真正深入理解

### 2.1 面试官追问模式

- "你怎么证明这个优化是有效的？"
- "实验是怎么设计的？"
- "baseline 是什么？"
- "指标是怎么选的？"
- "结果有没有统计显著性？"
- "有没有做消融实验？"

### 2.2 示例回答框架

#### 示例1：KV Cache 传输 vs 重计算的实验设计

**背景**：在 PD 分离架构下，KV Cache 需要从 Prefill 节点传输到 Decode 节点。问题是：传输是否一定比重算更快？

**回答结构**：

**实验目标**
> 验证在不同场景下（模型类型、序列长度、硬件配置），KV Cache 传输与重计算的性能对比

**变量设计**
> - 自变量：context_length、cache_len、seq_len
> - 因变量：总延迟（传输时间 vs 计算时间）
> - 控制变量：硬件配置、模型参数、batch size

**实验方法**
> 1. 理论计算：基于带宽和算力公式推导
>    ```
>    传输时间 = accessData / bandwidth
>    计算时间 = calcData / Flops
>    ```
> 2. 分别计算 MLA 模型和 GQA 模型的 crossover point
> 3. 考虑实际传输效率（0.5-0.7）和计算 MFU（0.4-0.5）

**关键发现**
> 1. **MLA 模型**：由于 KV Cache 压缩到 512 维，传输开销小，传输通常更快
> 2. **GQA 模型**：KV Cache 较大，长序列时重计算可能更快
> 3. **Crossover Point**：存在一个序列长度阈值，低于阈值传输更快，高于阈值重算更快

**追问应对**
> **Q：怎么确定传输效率和 MFU 的取值？**
> A：通过 profiling 工具实际测量。使用 `nvidia-smi` 监控 GPU 利用率，使用 `nvprof` 或 `nsys` 采集 kernel 级别的时间。参考 `sglang_profiling_from_scratch.ipynb` 中的方法。

> **Q：有没有考虑并行的情况？**
> A：实际系统中传输和计算可以 pipeline 并行。但为了简化分析，先假设串行。并行情况下需要更复杂的建模，考虑重叠度和气泡率。

#### 示例2：R3（Rollout Router Replay）对照实验

**背景**：在 RL 训练中，MoE 模型的路由决策在 rollout 和训练阶段可能不一致，R3 通过记录并回放路由决策来解决这个问题。

**实验设计**

**对照组设计**
> - R3 组：开启 `--use-rollout-routing-replay`
> - No-R3 组：不开启该 flag
> - 其他所有参数保持一致

**评估指标**
> - 主指标：AIME-2024 pass@16（数学推理能力）
> - 辅助指标：reward 曲线、训练稳定性、训推一致性

**实验配置**
> - 硬件：单机 8×GPU（Hopper）
> - 模型：Qwen3-30B-A3B（MoE）
> - 训练：GSPO 算法，200 steps
> - 评测：每 20 步评估一次

**结果分析**
> - R3 组的训推一致性更好（路由决策完全一致）
> - R3 组的 reward 曲线更稳定
> - No-R3 组可能出现训练不稳定或 reward 突然下降

**追问应对**
> **Q：为什么选择 pass@16 而不是 pass@1？**
> A：pass@16 更能反映模型的上限能力，减少采样随机性的影响。pass@1 容易受单次采样结果波动影响。

> **Q：200 步够吗？怎么确定训练收敛了？**
> A：200 步主要用于快速验证，观察趋势。可以通过 loss 曲线和 reward 曲线判断是否收敛。如果曲线趋于平稳，说明基本收敛。

### 2.3 实验设计能力检查清单

| 维度 | 检查点 | 示例 |
|------|--------|------|
| **目标明确** | 是否清楚要验证什么假设 | "验证 R3 是否能提高训推一致性" |
| **变量控制** | 是否只改变一个变量 | R3 vs No-R3，其他参数完全相同 |
| **Baseline 合理** | 对比基线是否公平 | 使用相同的模型、数据、超参 |
| **指标选择** | 是否选择合适的评估指标 | AIME pass@16 for math reasoning |
| **统计显著性** | 是否多次运行取平均 | 至少 3 次运行，报告均值和方差 |
| **消融实验** | 是否验证各组件的贡献 | 逐个关闭组件，观察性能变化 |

---

## 第三类：问题定位能力

> **核心要求**：模型上线后能力下降、系统变慢、实验结果不符预期，这些问题是怎么排查的

### 3.1 面试官追问模式

- "上线后性能突然下降，你怎么排查？"
- "推理服务突然变慢，你怎么定位瓶颈？"
- "实验结果和预期不一致，你怎么分析？"

### 3.2 问题排查框架

**系统化排查思路**：
```
1. 确认问题 → 2. 收集信息 → 3. 缩小范围 → 4. 定位根因 → 5. 验证修复
```

### 3.3 示例回答框架

#### 示例1：推理服务突然变慢

**面试官**：上线后推理服务突然变得很慢，你怎么排查？

**回答结构**：

**第一步：确认问题**
> - 是所有请求都慢，还是特定请求慢？
> - 是 TTFT（首 token 延迟）慢，还是 TPOT（每 token 延迟）慢？
> - 是突然变慢，还是逐渐变慢？

**第二步：收集信息**
> 1. **系统指标**：
>    - GPU 利用率：`nvidia-smi` 或 Prometheus
>    - 显存使用：是否有 OOM 风险
>    - CPU 使用：是否有 CPU 瓶颈
>    - 网络带宽：PD 分离场景下 KV Cache 传输
>
> 2. **应用指标**：
>    - 请求队列长度：是否有积压
>    - Batch size：是否达到最大 batch
>    - KV Cache 使用率：是否频繁换入换出
>
> 3. **日志分析**：
>    - 查看是否有错误日志
>    - 查看是否有超时
>    - 查看是否有抢占（preemption）

**第三步：缩小范围**
> 使用 profiling 工具定位瓶颈：
> - Python 层：cProfile、py-spy
> - GPU 层：nsys、ncu
> - 参考 `sglang_profiling_from_scratch.ipynb` 中的方法

**第四步：常见原因及解决方案**

| 现象 | 可能原因 | 排查方法 | 解决方案 |
|------|---------|---------|---------|
| TTFT 突然变长 | Prefill 被长请求阻塞 | 查看请求队列 | 启用 Chunked Prefill |
| TPOT 逐渐变长 | KV Cache 不足导致换入换出 | 查看显存使用 | 减小 max_num_seqs |
| GPU 利用率低 | Batch size 太小 | 查看 batch 统计 | 调整调度策略 |
| 显存持续增长 | KV Cache 泄漏 | 监控显存曲线 | 检查 Block 释放逻辑 |
| 偶发超时 | 某些请求特别长 | 分析请求分布 | 设置超时限制 |

**第五步：验证修复**
> - A/B 测试：新旧版本对比
> - 压力测试：模拟高并发场景
> - 监控告警：设置关键指标阈值

#### 示例2：RL 训练 reward 突然下降

**面试官**：RL 训练过程中 reward 突然下降，你怎么排查？

**回答结构**：

**第一步：确认问题**
> - 是所有任务的 reward 都下降，还是特定任务？
> - 是突然下降，还是逐渐下降？
> - 下降后是否能恢复？

**第二步：收集信息**
> 1. **训练指标**：
>    - Loss 曲线：是否有异常波动
>    - 梯度范数：是否有梯度爆炸/消失
>    - 学习率：是否按预期变化
>
> 2. **数据相关**：
>    - 数据分布：是否有数据质量问题
>    - 采样策略：是否引入偏差
>
> 3. **模型相关**：
>    - KL 散度：与参考模型的偏离程度
>    - 策略熵：探索程度是否合理

**第三步：常见原因及解决方案**

| 现象 | 可能原因 | 排查方法 | 解决方案 |
|------|---------|---------|---------|
| Reward 突然下降 | 梯度爆炸 | 查看梯度范数 | 梯度裁剪 |
| Reward 逐渐下降 | KL 散度过大 | 监控 KL 值 | 调整 KL 系数 |
| 训练不稳定 | 数据质量问题 | 检查数据分布 | 数据清洗 |
| 训推不一致 | 路由决策不一致 | 对比 rollout/training 路由 | 使用 R3 |

### 3.4 问题排查工具箱

**GPU 相关**：
```bash
# 实时监控 GPU
nvidia-smi -l 1

# 详细 profiling
nsys profile -o report python train.py

# Kernel 级别分析
ncu --set full python train.py
```

**PyTorch 相关**：
```python
# 显存 snapshot
torch.cuda.memory._record_memory_history()
# ... 运行代码 ...
torch.cuda.memory._dump_snapshot("snapshot.pickle")

# 梯度监控
torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
```

**vLLM 相关**：
```python
# 启用详细日志
import logging
logging.getLogger("vllm").setLevel(logging.DEBUG)

# 显存可视化
# 参考 vllm_mem_snapshot.ipynb
```

---

## 第四类：工程落地能力

> **核心要求**：理论可行的方案在实际工程落地中可能不可行，关键在理论结合实际

### 4.1 面试官追问模式

- "这个方案怎么部署？"
- "上线后怎么保证稳定性？"
- "怎么处理异常情况？"
- "如果资源有限，怎么优化？"

### 4.2 工程落地检查清单

| 维度 | 检查点 | 示例 |
|------|--------|------|
| **部署方案** | 如何部署到生产环境 | Docker + K8s + 负载均衡 |
| **稳定性** | 如何保证服务 SLA | 健康检查、自动重启、限流 |
| **监控告警** | 如何监控服务状态 | Prometheus + Grafana + 告警规则 |
| **异常处理** | 如何处理异常情况 | 降级策略、重试机制、超时控制 |
| **数据回滚** | 如何处理数据问题 | 版本管理、灰度发布、快速回滚 |
| **成本控制** | 如何优化资源使用 | 自动扩缩容、资源复用 |

### 4.3 示例回答框架

#### 示例1：vLLM 推理服务部署

**面试官**：怎么把一个大模型部署上线？

**回答结构**：

**1. 部署架构**
```
                    ┌─────────────┐
                    │   Load      │
                    │  Balancer   │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
     ┌──────▼──────┐ ┌─────▼───────┐ ┌────▼────────┐
     │  vLLM Node  │ │  vLLM Node  │ │  vLLM Node  │
     │  (TP=4)     │ │  (TP=4)     │ │  (TP=4)     │
     └─────────────┘ └─────────────┘ └─────────────┘
```

**2. 部署步骤**
```bash
# 1. 容器化
docker build -t llm-service:v1 .

# 2. 启动服务
docker run -d --gpus all \
  -v /models:/models \
  -p 8000:8000 \
  llm-service:v1 \
  --model /models/Qwen3-30B-A3B \
  --tensor-parallel-size 4 \
  --max-model-len 32768 \
  --gpu-memory-utilization 0.9

# 3. 健康检查
curl http://localhost:8000/health
```

**3. 稳定性保障**

| 措施 | 实现方式 | 作用 |
|------|---------|------|
| 健康检查 | /health 端点 | 自动检测服务状态 |
| 限流 | Rate Limiting | 防止过载 |
| 超时控制 | 请求超时设置 | 防止长时间阻塞 |
| 自动重启 | K8s restart policy | 故障自动恢复 |
| 优雅关闭 | SIGTERM 处理 | 避免请求丢失 |

**4. 监控告警**
```yaml
# Prometheus 指标
- vllm:request_success_total  # 请求成功数
- vllm:time_to_first_token    # TTFT
- vllm:time_per_output_token  # TPOT
- vllm:gpu_cache_usage        # KV Cache 使用率

# 告警规则
- alert: HighLatency
  expr: vllm:time_to_first_token > 2
  for: 5m
```

**5. 异常处理**
```python
# 降级策略
if queue_length > threshold:
    return cached_response(request)

# 重试机制
@retry(max_attempts=3, backoff=exponential)
def call_llm_service(request):
    ...

# 超时控制
response = llm.generate(prompt, timeout=30)
```

#### 示例2：PD 分离架构部署

**面试官**：PD 分离架构怎么部署？需要注意什么？

**回答结构**：

**1. 架构设计**
```
     ┌─────────────┐
     │   Router    │
     └──────┬──────┘
            │
     ┌──────▼──────┐         ┌─────────────┐
     │   Prefill   │ ──KV──▶ │   Decode    │
     │   Nodes     │  Cache  │   Nodes     │
     └─────────────┘         └─────────────┘
```

**2. 关键技术点**
> - **KV Cache 传输**：使用 NCCL 或 RDMA 高速传输
> - **负载均衡**：根据 Prefill/Decode 负载动态调度
> - **故障处理**：Prefill 节点故障时重试，Decode 节点故障时迁移

**3. 性能优化**
> - **KV Cache 压缩**：使用 MLA 或量化减少传输量
> - **传输与计算重叠**：Pipeline 并行
> - **Prefix Cache**：减少重复 Prefill

**4. 稳定性保障**
> - **心跳检测**：监控各节点状态
> - **超时重试**：KV Cache 传输超时时重试或重算
> - **降级方案**：PD 分离不可用时降级为单机模式

#### 示例3：RL 训推共卡部署

**面试官**：训推共卡模式怎么实现？有什么挑战？

**回答结构**：

**1. 核心挑战**
> - 训练和推理共享 GPU 显存，需要动态分配
> - 切换时需要卸载/加载模型，有时间开销
> - 需要保证训推一致性

**2. 实现方案**
```python
# 参考 rl/training_infer_colocate.ipynb

# 1. Ray 资源调度
from ray.util.placement_group import placement_group
pg = placement_group([{"GPU": 1}] * 8, strategy="STRICT_PACK")

# 2. 训练阶段
# 卸载推理模型 → 加载训练模型 → 训练

# 3. 推理阶段
# 卸载训练模型 → 加载推理模型 → 推理

# 4. 权重同步
# 使用 CUDA IPC 进程间通信
```

**3. 显存管理**
> - **Offload to CPU**：将不活跃的模型卸载到 CPU
> - **Gradient Checkpointing**：用计算换显存
> - **torch_memory_saver**：CUDA Hook 方式卸载

**4. 稳定性保障**
> - **显存监控**：实时监控显存使用，避免 OOM
> - **超时处理**：训练/推理超时时强制切换
> - **状态恢复**：异常时恢复到上一个稳定状态

### 4.4 常见工程问题及解决方案

| 问题 | 场景 | 解决方案 |
|------|------|---------|
| OOM | 显存不足 | 减小 batch size、启用 offload、使用量化 |
| 延迟过高 | 请求积压 | 扩容、启用 Chunked Prefill、调整调度策略 |
| 服务不可用 | 节点故障 | 健康检查、自动重启、多副本 |
| 数据丢失 | 异常退出 | 持久化 checkpoint、日志记录 |
| 成本过高 | 资源浪费 | 自动扩缩容、Spot 实例、资源复用 |

---

## 第五类：业务与实际场景理解

> **核心要求**：方案适合什么场景、用户关心什么、上线成本有多高、资源有限时优先优化什么

### 5.1 面试官追问模式

- "这个方案适合什么样的业务场景？"
- "用户更关心的是什么？"
- "上线成本有多高？"
- "如果资源有限，我们应该首先优化哪些部分？"

### 5.2 场景分析框架

**场景维度**：
| 维度 | 考虑点 | 示例 |
|------|--------|------|
| **用户类型** | B端/C端、开发者/普通用户 | API 服务 vs 聊天机器人 |
| **使用模式** | 实时/离线、交互式/批处理 | 在线客服 vs 文档处理 |
| **性能要求** | 延迟、吞吐、可用性 | 实时翻译需要低延迟 |
| **成本预算** | GPU 成本、运维成本 | 初创公司 vs 大厂 |
| **数据特点** | 长度分布、并发量、峰值 | 短文本高并发 vs 长文档低并发 |

### 5.3 示例回答框架

#### 示例1：推理框架选型

**面试官**：vLLM 和 SGLang 怎么选？

**回答结构**：

**场景对比**

| 场景 | 推荐方案 | 原因 |
|------|---------|------|
| 多轮对话 | SGLang | RadixAttention 自动复用前缀 |
| 批量处理 | vLLM | Continuous Batching 吞吐高 |
| 长上下文 | SGLang | 更好的 Prefix Caching |
| 多 LoRA 服务 | vLLM | Multi-LoRA 支持成熟 |
| 代码生成 | 两者皆可 | 关注 TTFT 和 TPOT |

**成本分析**
> - **开发成本**：vLLM 社区更大，文档更全
> - **运维成本**：两者都支持 OpenAI API，运维差异不大
> - **硬件成本**：相同模型下，GPU 需求基本一致

**决策建议**
> 1. 如果主要场景是多轮对话/Agent，优先 SGLang
> 2. 如果主要场景是批量处理/高吞吐，优先 vLLM
> 3. 如果团队有 vLLM 经验，继续用 vLLM，迁移成本高

#### 示例2：MoE 模型部署方案

**面试官**：MoE 模型怎么部署？成本和效果怎么平衡？

**回答结构**：

**MoE 部署的挑战**
> 1. **显存开销**：总参数大（671B），需要多卡加载
> 2. **通信开销**：Expert Parallelism 需要 All-to-All 通信
> 3. **负载不均**：热门 Expert 可能成为瓶颈

**部署方案对比**

| 方案 | 显存需求 | 通信开销 | 适用场景 |
|------|---------|---------|---------|
| TP=8 | 低 | 高（AllReduce） | 单机 8 卡 |
| EP=8 | 中 | 高（All-to-All） | 多机 |
| TP=4 + EP=2 | 中 | 中 | 折中方案 |

**成本优化**
> 1. **量化**：INT8/FP8 减少显存和计算量
> 2. **稀疏激活**：只加载需要的 Expert（需要特殊支持）
> 3. **批处理**：提高 GPU 利用率

**资源有限时的优先级**
> 1. **首先**：确保模型能跑起来（显存足够）
> 2. **其次**：优化吞吐（调整 batch size、调度策略）
> 3. **然后**：优化延迟（Chunked Prefill、投机推理）
> 4. **最后**：优化成本（量化、扩缩容）

#### 示例3：RL 训练方案选择

**面试官**：RL 训练方案怎么选？DPO 还是 PPO？

**回答结构**：

**方案对比**

| 维度 | DPO | PPO |
|------|-----|-----|
| **实现复杂度** | 低 | 高 |
| **训练稳定性** | 高 | 中 |
| **效果上限** | 中 | 高 |
| **资源需求** | 低 | 高（需要 4 个模型） |
| **适用场景** | 偏好对齐 | 复杂奖励信号 |

**场景选择**
> - **资源有限 + 简单任务**：DPO
> - **资源充足 + 复杂任务**：PPO
> - **需要在线学习**：PPO
> - **只有离线数据**：DPO

**成本分析**
> - **PPO**：需要 Policy Model、Reference Model、Reward Model、Value Model
> - **DPO**：只需要 Policy Model 和 Reference Model
> - **训推共卡**：可以节省一半 GPU

### 5.4 业务价值评估框架

**价值维度**：
| 维度 | 评估指标 | 示例 |
|------|---------|------|
| **用户体验** | 延迟、准确率、满意度 | TTFT < 2s, 准确率 > 90% |
| **成本节约** | GPU 成本、人力成本 | 节省 30% GPU 成本 |
| **效率提升** | 吞吐、并发数 | 吞吐提升 2x |
| **风险控制** | 可用性、数据安全 | SLA 99.9% |

**ROI 计算**：
```
ROI = (收益 - 成本) / 成本

收益 = 用户增长 × 单用户价值 + 成本节约
成本 = GPU 成本 + 开发成本 + 运维成本
```

### 5.5 资源有限时的优先级排序

**原则**：先解决瓶颈，再优化细节

**优先级矩阵**：
| 优先级 | 优化方向 | 预期收益 | 实施难度 |
|--------|---------|---------|---------|
| P0 | 模型能跑起来 | 基础功能 | 低 |
| P1 | 满足 SLA 要求 | 用户体验 | 中 |
| P2 | 优化吞吐 | 成本节约 | 中 |
| P3 | 优化延迟 | 用户体验 | 高 |
| P4 | 深度优化 | 边际收益递减 | 高 |

**示例：资源有限时的优化路径**
```
阶段1（1周）：确保模型能跑
  - 选择合适的 TP/PP 配置
  - 启用基本的 KV Cache

阶段2（2周）：满足基本 SLA
  - 启用 Chunked Prefill
  - 调整 batch size 和调度策略
  - 设置超时和限流

阶段3（1个月）：优化成本
  - 启用量化（INT8/FP8）
  - 优化 Prefix Caching
  - 启用自动扩缩容

阶段4（持续）：深度优化
  - 投机推理
  - Custom Kernel
  - 硬件升级
```

---

## 综合面试场景模拟

### 场景1：MLA 优化项目

**面试官**：你做了 MLA 的优化，讲讲这个项目？

**回答结构**：

**1. 背景与问题**（业务理解）
> DeepSeek V3 使用 MLA 机制，但在 Prefill 阶段仍需计算完整的 QKV，计算量大。目标是降低 Prefill 延迟，提升用户体验。

**2. 方案设计**（底层原理）
> 分析 MLA 的计算流程，发现 Prefill 阶段可以使用 MHA 模式，Decode 阶段使用 MQA 模式。通过吸收矩阵优化，减少显式矩阵乘法。

**3. 实验验证**（实验能力）
> - Baseline：标准 MHA
> - 指标：Prefill 延迟、显存使用、MFU
> - 结果：延迟降低 30%，显存减少 40%

**4. 工程落地**（工程能力）
> - 部署：集成到 vLLM
> - 监控：TTFT、GPU 利用率
> - 稳定性：A/B 测试、灰度发布

**5. 问题排查**（问题定位）
> - 问题：上线后某些场景延迟反而增加
> - 排查：Profiling 发现是 kernel launch 开销
> - 解决：使用 CUDA Graph 合并 kernel

**6. 业务价值**（业务理解）
> - 用户体验：TTFT 从 3s 降到 2s
> - 成本：相同 GPU 下 QPS 提升 30%
> - 适用场景：长上下文对话、文档处理

### 场景2：推理服务性能优化

**面试官**：推理服务上线后变慢了，你怎么优化？

**回答结构**：

**1. 问题定位**（问题排查）
> - 确认现象：TPOT 从 50ms 增加到 200ms
> - 收集信息：GPU 利用率 95%，显存使用 90%
> - Profiling：发现 KV Cache 换入换出频繁

**2. 根因分析**（底层原理）
> - 原因：并发请求增加，KV Cache 不足
> - 原理：vLLM 的 PagedAttention 需要预分配 Block

**3. 解决方案**（工程落地）
> - 短期：减小 max_num_seqs，限制并发
> - 中期：启用 Chunked Prefill，减少单请求显存占用
> - 长期：扩容，增加 GPU

**4. 验证效果**（实验能力）
> - 压力测试：模拟 100 并发
> - 结果：TPOT 恢复到 60ms，P99 < 100ms

**5. 长期优化**（业务理解）
> - 监控告警：设置 TPOT 阈值
> - 自动扩缩容：根据负载动态调整
> - 成本优化：使用 Spot 实例

---

## 总结：五类能力的自我检查

| 能力 | 检查问题 | 你的准备 |
|------|---------|---------|
| **底层原理** | 能否讲清楚技术的"为什么"而不只是"是什么" | ☐ |
| **实验验证** | 能否讲清楚实验设计和结果分析 | ☐ |
| **问题定位** | 能否讲清楚排查问题的思路和方法 | ☐ |
| **工程落地** | 能否讲清楚从理论到实践的挑战和解决方案 | ☐ |
| **业务理解** | 能否讲清楚方案的适用场景和价值 | ☐ |

---

*本文档基于 InfraTech 项目深度分析，最后更新：2026年6月*

*准备建议：针对每个技术点，按照"四层追问"框架准备回答，确保能应对面试官的层层深入追问*
